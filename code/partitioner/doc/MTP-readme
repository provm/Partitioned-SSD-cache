What changes are made to flashcache?

Relaim.c is left.................
rebalance lRU, intit lru
1. flashcache.h : made cache_set double pointer
				intiliased number of continers  hardcoded in #define .
				void flashcache_clean_set(struct cache_c *dmc, int set, int force_clean_blocks, int container);
				added continer id to strut cacheblock
				int flashcache_invalid_get(struct cache_c *dmc, int set, int container);
				flashcache_hash_lookup
				flashcache_invalid_insert(
				flashcache_merge_writes
				flashcache_invalid_remove
				flashcache_hash_insert // added in conf to intiliase continer id as 0 in cacheblks
				
2. flashcache_conf.c : changing the intilialisation of cache_sets
					
					
3.  flashcache_main.c (8): struct kcached_job *job = (struct kcached_job *)context;
			PROBLEM :  finding container from index.
					    if two containers map blcok to same set then index will be same, data will be overritten
					    find_valid_dbn(
					    
4. flashcache_reclaim(all) :  everything is on index basis no bio used anywhere
					either change function prototype and pass conatiner id or get coantiner id from index.
					
5.  flashcache_subr.c (7): 

6. flashcache_kcopy : flashcache_copy_job has no bio pramater 
					using continer id form cacheblock

check if set_ix is different for every container 
index is unique for every container
problem : if different containers get same sets then reclaimation will be basied
